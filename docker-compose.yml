services:
  postgres:
    image: postgres:16
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: linkupgo
      POSTGRES_USER: linkupgo
      POSTGRES_PASSWORD: linkupgo
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
